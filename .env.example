# App
APP_ENV=local

# Database (local dev) - replace with your own values in .env
DB_URL="jdbc:postgresql://localhost:5432/newtrinity_dev"
DB_USER=postgres
DB_PASSWORD=changeme
DB_POOL_SIZE=10

# UGS (override if needed)
# UGS_PROJECT_ID=
# UGS_ISSUER=
# UGS_JWKS_URL=
